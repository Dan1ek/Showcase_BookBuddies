@* @model Showcase_BookBuddies.Business.Entities.BookList *@

@{
    ViewData["Title"] = "DeleteLibrary";
}

<h1>DeleteLibrary</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>BookList</h4>
@*     <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ListTitle)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ListTitle)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ListDescription)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ListDescription)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Id)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Id)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.UserId)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.UserId)
        </dd>
    </dl> *@
    
    
</div>





@foreach (var item in Model)
{
    <book-list list-title="@item.ListTitle" list-description="@item.ListDescription" list-id="@item.Id"></book-list>
    foreach (var book in item.Books)
    {
        <book-s book-title="@book.BookTitle" book-author="@book.BookAuthor"></book-s>
    }
    <form asp-action="DeleteBookList" asp-controller="Home" method="post">
        <input type="hidden" name="listId" value="@item.Id" />
        <button id="deleteLibraryButton">Delete</button>
    </form>
}